---
name: ddd-parallel-orchestration
description: 在项目工作开始时使用；主代理只负责编排，通过共享的项目领域边界规则协调子代理，并将所有实质性工作委派出去。
---

# ddd 并行编排

## 概览

开始任何项目工作时，先检查领域边界，再把所有实质性工作分派给子代理。主代理必须只承担编排职责：已有 `.domain-boundaries.md` 时直接复用；缺失或需要调整时，通过子代理创建或更新；调查、实现、验证与边界维护都通过委派完成。

## 核心规则

1. 主代理不得亲自执行实质性任务。调查、实现、验证、边界文件编辑都必须委派给子代理。
2. 在规划子代理工作前，先检查项目根目录下的 `.domain-boundaries.md`。
3. 只要已有可用边界，就应复用；没有充分理由时，不要重新推断边界。
4. 如果文件缺失，在开始并行写入工作前，先委派子代理创建一个最小可用版本。
5. 任务拆分应按限界上下文或所有权划分，而不是按文件数量机械切分。
6. 优先并行化读取、调查、审查和验证任务。
7. 只有当所有权明确且写入范围不重叠时，才允许并行写任务。
8. 主代理只负责编排、冲突处理、结果集成和完成判断。
9. 当任务暴露出更准确的边界信息时，应通过委派更新 `.domain-boundaries.md`。
10. 项目任务默认派发给子代理；即使只有一条实质性工作流，也应派一个子代理执行，而不是由主代理亲自做。
11. 如果当前会话的运行时工具策略、审批策略、产品策略或能力状态不允许使用 `spawn_agent`，主代理必须立即报告冲突或阻塞，不得静默降级为自己亲自完成原本应委派的实质性任务。
12. 只有用户随后明确接受“这次由主代理直接执行”的回退方案后，主代理才可以接管原本应委派的实质性工作。

## 边界文件

使用项目根目录下的 `.domain-boundaries.md` 作为项目边界地图。

### 必需结构

文件应保持简洁、结构化，并足够稳定以支持增量更新。使用标题编号作为区块定位依据，便于代理快速定位各节；不要额外写独立的区块编号字段。领域边界文件整体应使用中文描述；路径、模块名、接口名等代码标识符保留原文。

```md
# 领域边界

## 0. 目录

- [1. 项目](#1-项目)
- [2. 全局规则](#2-全局规则)
- [3. 限界上下文](#3-限界上下文)
  - [3.1 示例上下文](#31-示例上下文)
- [4. 共享区域](#4-共享区域)
- [5. 协调规则](#5-协调规则)
- [6. 待确认问题](#6-待确认问题)
- [7. 变更记录](#7-变更记录)

## 1. 项目

- 名称: <项目名称>
- 最后更新: <yyyy-mm-dd>
- 来源: 人工定义 | 代理推断 | 混合

## 2. 全局规则

- 优先复用已有边界，而不是重新推断。
- 不要把存在所有权重叠的范围分配为并行写任务。

## 3. 限界上下文

### 3.1 示例上下文

- 目标: <这个上下文负责什么>
- 不负责: <它不应修改或决策什么>
- 主要路径:
  - path/a
- 关键模块:
  - module/or/package
- 对外接口:
  - 导出的契约、api、事件
- 上游依赖:
  - 其他上下文
- 下游依赖方:
  - 其他上下文
- 并行工作规则:
  - 可安全读取: yes|no
  - 可安全写入: yes|no
  - 备注: <约束>

## 4. 共享区域

- 路径: <共享路径>
- 风险: low|medium|high
- 规则: <代理应如何接触这里>

## 5. 协调规则

- 按限界上下文拆分工作。
- 跨上下文重构默认串行执行。
- 写入共享区域前必须经过主代理批准。

## 6. 待确认问题

- <尚不明确的边界>

## 7. 变更记录

- <date> <summary>
```

### 边界文件规则

1. 先读 `0. 目录`，再按区块编号跳转。
2. 引用内容时使用区块编号和名称，不要用模糊描述。
3. 新增或重命名章节时，要同步更新目录和区块编号。
4. 把 `待确认问题` 当作不确定信息的存放区，不要把低置信度猜测写成既定结构。
5. 除代码标识符外，领域边界文件的标题、说明、规则、上下文描述都应使用中文。

## 编排流程

1. 先读取项目根目录下的 `.domain-boundaries.md`。
2. 如果文件不存在，派一个边界分析子代理推断最小可用领域地图并创建它。
3. 把任务拆成一条或多条委派工作流；如果只有一条实质性工作流，也应派一个子代理去做。
4. 能拆时尽量按限界上下文、所有权或真正独立的职责拆分。
5. 即便某个实质性工作卡在关键路径上，也不要留在主代理本地执行；应派出对应子代理，主代理负责等待期间的协调。
6. 对独立的侧边工作流，额外派发范围明确的子代理。
7. 集成结果、解决冲突；如果学到了新的结构事实，再派后续边界更新任务。

## 派发规则

每个子代理任务都要明确写出：

- 目的
- 只读还是可写
- 负责的一个或多个上下文或路径，以及 `.domain-boundaries.md` 中对应的行号范围
- 主代理已知与任务关联的文件列表及行范围
- 禁止触碰的区域
- 预期产出
- 如何避免与其他代理或用户改动重叠

如果某个可写子任务的所有权边界还不能用一小段话说清楚，就不要派发。
不要因为任务看起来小、急，或在本地做更顺手，就把实质性工作留给主代理。

## 派发消息模板

每次分派子代理都使用这个模板。保持简洁，但每个字段都要填。

```text
task: <一个明确目标>

role:
- 你是一个在主代理编排下工作的子代理。

boundary:
- contexts:
  - <.domain-boundaries.md:l42-l68 上下文名称或区块编号>
- known related files:
  - <主代理已知与任务关联的文件及行范围，例如 path/file.go:l10-l42；没有则 "none">
- allowed paths:
  - <具体文件、目录或模块>
- forbidden paths:
  - <共享区域、其他上下文，或 "none">
- mode: read-only | writable

goal:
- deliver: <必须交付什么>
- stop condition: <何时停止，而不是继续扩展>

constraints:
- 不要超出允许边界。
- 不要回退用户改动或其他代理改动。
- 如果边界看起来错误或不完整，明确报告。
- 如果任务需要跨入其他上下文，停止并上报。

output:
- summary: <简短结果总结>
- files changed: <列表或 "none">
- risks / unknowns: <列表或 "none">
- boundary update suggestion: <对 .domain-boundaries.md 的必要修改，或 "none">
```

如果任务可写，再加一行：

```text
- ownership reason: <为什么这个子代理是该可写范围的正确所有者>
```

如果任务是审查或验证，把 `files changed` 替换成：

```text
- findings: <缺陷、风险、缺失测试，或 "none">
```

## 适合并行的工作

- 在不同限界上下文中分别做代码探索
- 在彼此独立的上下文中实现功能
- 一个实现任务加一个独立审查任务
- 不修改同一批产物的独立验证任务
- 由一个子代理处理单条关键路径任务，主代理负责协调

## 不适合并行的工作

- 多个代理同时写同一模块或共享文件
- 两个代理分别实现一个尚未稳定的契约两端
- 主代理自己做调查、实现、验证或直接维护边界文件
- 在已有可用边界文件时重复做领域分析

## 集成规则

1. 不要在本地重复做已经委派出去的工作。
2. 集成前先检查返回结果是否违反边界。
3. 如果结果在假设、所有权或契约上发生冲突，就停止继续并行扩张。
4. 最终完成判断始终由主代理负责。

## 边界更新

当你确认了以下任一信息时，应更新 `.domain-boundaries.md`：

- 出现了新的限界上下文
- 某个路径或模块的所有权更清晰了
- 某个共享区域的风险比之前判断更高
- 某个跨上下文接口已经明确
- 旧的边界描述是错误的

优先做小范围更新。除非当前结构已经不可用，否则不要整体重写文件。
